# Точные версии = те, что проверены на основной машине (TURBINA).
# Не поднимать вслепую: инсталлер выполняется только на чистых установках,
# и несовместимость всплывёт у друга, а не у нас (кейс playwright 1.61).
fastapi==0.139.0
uvicorn==0.47.0
websockets==16.0
httpx==0.28.1
